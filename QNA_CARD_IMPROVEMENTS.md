# QnA 카드 레이아웃 개선 사항

## 완료된 작업

### 1. QnA 카드 레이아웃 재구성
- **좌측 아이콘 영역**: 카테고리별 큰 아이콘(80% 높이) 표시
  - 각 카테고리별 고유 색상 배경 적용
  - 아이콘 크기: 32-40px (반응형)
  - 배경색: 카테고리별 맞춤 색상

- **우측 콘텐츠 영역**: 
  - 상단: 카테고리 태그 + 상태 배지
  - 제목: 최대 2줄 표시
  - 메타 정보: 작성자, 날짜, 대학, 전공
  - 콘텐츠 미리보기: 최대 2줄
  - 하단: 조회수, 추천수, 답변 수 통계

### 2. 카드 스타일 개선
- **호버 효과**:
  - 스케일: 1.02배 확대
  - 그림자: 강화된 그림자 효과
  - 위치: -1px 위로 이동
  - 테두리: 브랜드 컬러로 강조

- **반응형 디자인**:
  - 모바일: 패딩 감소, 텍스트 크기 축소
  - 데스크톱: 충분한 여백과 가독성

### 3. 카테고리 태그 표시
- 각 카드의 우측 상단에 카테고리 태그 표시
- 배지 스타일: 회색 배경 + 검은색 텍스트

### 4. 상태 배지 통합
- 상태별 배지 표시:
  - 답변대기: "pending" 상태
  - 답변완료/해결: "accepted" 상태
  - 기타: "new" 상태

### 5. 통계 정보 표시
- 조회수 (Eye 아이콘)
- 추천수 (ThumbsUp 아이콘)
- 답변 수 (MessageCircle 아이콘)

## 기술적 개선사항

### 1. Radix UI Tabs 제거
- `Tabs.Root` 및 `Tabs.Trigger` 컴포넌트 제거
- 일반 `<button>` 요소로 대체
- RovingFocusGroup 관련 에러 해결

### 2. 컴포넌트 구조
- 카드를 `flex` 레이아웃으로 구성
- 좌측: `flex-shrink-0` (고정 너비)
- 우측: `flex-1` (유연한 너비)

## 파일 변경사항

### client/src/pages/QnAList.tsx
- 카드 렌더링 로직 완전 재작성
- Tabs 컴포넌트 제거
- 새로운 카드 레이아웃 구현
- 반응형 디자인 적용

## 다음 단계

1. **조회수 증가 로직**: QnA 상세 페이지 접근 시 viewCount 증가
2. **추천 기능**: 좋아요 버튼 클릭 시 likeCount 증가
3. **동적 상태**: 조회수/추천수 기반 "인기", "멘토 열람 중" 등 상태 자동 결정
4. **카테고리 필터링**: 좌측 아이콘 클릭 시 해당 카테고리로 필터링
5. **모바일 반응성**: 모바일 기기에서 레이아웃 검증

## 브라우저 테스트 결과

✅ QnA 페이지 정상 로드
✅ 새로운 카드 레이아웃 표시
✅ 좌측 아이콘 색상 적용
✅ 우측 콘텐츠 정렬
✅ 호버 효과 작동
✅ TypeScript 0 errors
✅ 개발 서버 정상 작동
