# 메시지 기능 UX 분석 및 개선안

## 현재 상태 분석

### 현재 구현된 기능
- ✅ 1:1 채팅 기능
- ✅ 상담 일정 제안 카드 (proposal)
- ✅ 상담 상태 표시 (pending, accepted, rejected, counter_proposed, cancelled, completed)
- ✅ 메시지 검색 기능
- ✅ 자동 스크롤 (새 메시지 도착 시)
- ✅ 타임스탐프 표시
- ✅ 반응형 레이아웃 (모바일/데스크톱)

---

## 벤치마킹: 주요 메시징 플랫폼 비교

### 1. 카카오톡 (KakaoTalk)
**강점:**
- 직관적이고 간단한 UI
- 빠른 메시지 전송
- 읽음 표시 (상대방이 메시지를 읽었는지 명확히 표시)
- 프로필 사진 표시로 시각적 구분
- 메시지 그룹화 (연속된 메시지를 한 그룹으로 표시)
- 이모지 반응 기능
- 메시지 검색 기능

### 2. Slack
**강점:**
- 스레드 기능 (특정 메시지에 대한 대화 분리)
- 메시지 반응 (emoji reactions)
- 메시지 수정/삭제 기능
- 파일 공유 및 미리보기
- 메시지 고정 기능
- 검색 필터링 (사용자, 날짜, 키워드)
- 메시지 포맷팅 (bold, italic, code block)

### 3. Discord
**강점:**
- 명확한 온/오프라인 상태 표시
- 메시지 스레드 기능
- 반응 시스템 (emoji reactions)
- 메시지 수정/삭제
- 타이핑 표시기 (상대방이 입력 중임을 표시)
- 메시지 핀 기능
- 풍부한 임베드 지원

### 4. WhatsApp
**강점:**
- 간단하고 빠른 UI
- 읽음 표시 (더블 체크)
- 마지막 본 시간 표시
- 메시지 검색
- 메시지 반응 (emoji reactions)
- 메시지 포워드 기능

---

## 유니브매치 메시지 기능의 문제점 및 개선안

### 문제점 1: 읽음 표시 부재
**현재 상황:**
- 상대방이 메시지를 읽었는지 알 수 없음
- 메시지가 전송되었는지만 확인 가능

**개선안:**
```
메시지 상태 표시:
- 📤 전송됨 (서버에 저장됨)
- 👁️ 읽음 (상대방이 메시지를 읽음)
- ⏱️ 배달됨 (상대방의 기기에 도착)
```

**구현 방법:**
- `isRead` 필드를 활용하여 읽음 상태 표시
- 메시지 시간 옆에 작은 아이콘으로 표시

---

### 문제점 2: 타이핑 표시기 부재
**현재 상황:**
- 상대방이 입력 중인지 알 수 없음
- 메시지 도착을 기다리는 동안 답답함

**개선안:**
```
타이핑 표시:
"테스트 멘토님이 입력 중입니다..."
```

**구현 방법:**
- WebSocket 또는 실시간 업데이트로 입력 상태 전송
- 3초 이상 입력이 없으면 자동으로 표시 해제

---

### 문제점 3: 메시지 그룹화 부재
**현재 상황:**
- 연속된 메시지가 각각 개별 버블로 표시됨
- 시각적으로 복잡하고 공간 낭비

**개선안:**
```
같은 사용자의 연속된 메시지를 그룹화:
- 5분 이내의 메시지는 하나의 그룹으로 표시
- 타임스탐프는 그룹의 마지막 메시지에만 표시
- 프로필 사진은 그룹의 첫 메시지에만 표시
```

**구현 방법:**
- 메시지 렌더링 시 이전 메시지의 사용자와 시간 비교
- 같은 사용자이고 5분 이내면 `isGrouped` 플래그 설정

---

### 문제점 4: 메시지 검색 기능 미흡
**현재 상황:**
- 대화 목록에서만 검색 가능
- 메시지 내용으로 검색 불가능

**개선안:**
```
전체 메시지 검색:
- 현재 대화에서 메시지 검색
- 검색 결과 하이라이트
- 검색 결과 네비게이션 (이전/다음)
```

**구현 방법:**
- 메시지 영역에 검색 입력창 추가
- 클라이언트 사이드 검색 또는 API 검색

---

### 문제점 5: 메시지 수정/삭제 기능 부재
**현재 상황:**
- 잘못 보낸 메시지를 수정하거나 삭제할 수 없음
- 사용자 경험 저하

**개선안:**
```
메시지 액션 메뉴:
- 수정 (메시지 전송 후 5분 이내)
- 삭제 (모두에게 삭제)
- 복사
- 포워드
```

**구현 방법:**
- 메시지 호버 시 액션 버튼 표시
- 수정/삭제 API 구현
- 수정된 메시지에 "(수정됨)" 표시

---

### 문제점 6: 프로필 사진 표시 부재
**현재 상황:**
- 메시지만 표시되고 사용자 구분이 어려움
- 시각적 피드백 부족

**개선안:**
```
메시지 옆에 프로필 사진 표시:
- 같은 사용자의 연속 메시지는 프로필 사진 생략
- 다른 사용자의 메시지는 프로필 사진 표시
- 프로필 사진 클릭 시 사용자 정보 표시
```

**구현 방법:**
- 사용자 정보 쿼리에서 프로필 사진 URL 포함
- 메시지 렌더링 시 프로필 사진 표시

---

### 문제점 7: 메시지 반응 (Emoji Reactions) 부재
**현재 상황:**
- 이모지로 빠르게 반응할 수 없음
- 감정 표현이 제한적

**개선안:**
```
메시지에 이모지 반응 추가:
- 메시지 호버 시 반응 버튼 표시
- 자주 사용하는 이모지 (👍, ❤️, 😂, 😮, 😢, 🔥)
- 이모지 클릭 시 반응 추가/제거
```

**구현 방법:**
- `messageReactions` 테이블 추가
- 메시지 아래에 반응 표시
- 반응 클릭 시 API 호출

---

### 문제점 8: 메시지 시간 표시 개선 필요
**현재 상황:**
- 상대 시간 표시만 있음 ("2분 전")
- 정확한 시간을 알기 어려움

**개선안:**
```
호버 시 정확한 시간 표시:
- 상대 시간: "2분 전"
- 호버 시: "2026-03-19 14:30:45"
```

**구현 방법:**
- `title` 속성에 정확한 시간 추가
- 또는 Tooltip 컴포넌트 사용

---

### 문제점 9: 상담 제안 카드의 UX 개선
**현재 상황:**
- 상담 제안 카드가 메시지와 구분이 명확하지 않음
- 카드 내 정보가 너무 많음

**개선안:**
```
상담 제안 카드 개선:
1. 카드 배경색 강화 (더 눈에 띄는 색상)
2. 정보 우선순위 정렬:
   - 상태 (수락 대기 중, 확정됨 등)
   - 날짜/시간
   - 상담 유형
   - 장소/온라인 여부
3. 버튼 배치 개선:
   - 모바일에서 버튼이 한 줄에 맞도록 조정
   - 버튼 크기 증가
```

**구현 방법:**
- 카드 배경색 더 진하게 설정
- 모바일 반응형 버튼 레이아웃 추가

---

### 문제점 10: 대화 목록 개선
**현재 상황:**
- 읽지 않은 메시지 개수 표시 없음
- 마지막 메시지 미리보기가 너무 짧음

**개선안:**
```
대화 목록 개선:
1. 읽지 않은 메시지 개수 배지 표시
2. 마지막 메시지 미리보기 확대 (현재: 1줄 → 개선: 2줄)
3. 온라인 상태 표시 (초록색 점)
4. 중요 대화 핀 기능
```

**구현 방법:**
- 읽지 않은 메시지 개수 계산 로직 추가
- 미리보기 텍스트 길이 증가
- 온라인 상태 API 추가

---

## 우선순위별 개선안

### Phase 1 (필수)
1. ✅ 읽음 표시 기능
2. ✅ 메시지 그룹화
3. ✅ 프로필 사진 표시

### Phase 2 (권장)
4. ✅ 타이핑 표시기
5. ✅ 메시지 수정/삭제
6. ✅ 메시지 반응

### Phase 3 (선택)
7. ✅ 메시지 검색 강화
8. ✅ 대화 목록 개선
9. ✅ 상담 제안 카드 개선

---

## 구현 체크리스트

- [ ] 읽음 표시 기능 구현
- [ ] 메시지 그룹화 로직 추가
- [ ] 프로필 사진 표시
- [ ] 타이핑 표시기 구현
- [ ] 메시지 수정/삭제 기능
- [ ] 메시지 반응 기능
- [ ] 메시지 검색 강화
- [ ] 대화 목록 개선
- [ ] 상담 제안 카드 UX 개선
- [ ] 모바일 반응형 테스트
- [ ] 성능 최적화 (메시지 가상화)
- [ ] 테스트 작성 및 검증
